// src/components/ui/feedback/Message/index.ts
import messageApi from './Message'; // Import the API object

// Export the API object as the default export for `import message from './Message'`
export default messageApi;

// Optionally, if you have a MessageProvider or types to export:
// export { MessageProvider } from './Message'; // If you created a provider
// export type { MessageArgsProps, MessageInstance } from './Message'; // If you have specific types to export
// For now, only the API object is exported as default.
