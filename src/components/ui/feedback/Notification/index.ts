// src/components/ui/feedback/Notification/index.ts
import notificationApi from './Notification'; // Import the API object

// Export the API object as the default export for `import notification from './Notification'`
export default notificationApi;

// Optionally, if you have a NotificationProvider or types to export:
// export { NotificationProvider } from './Notification'; // If you created a provider
// export type { NotificationArgsProps, NotificationInstance } from './Notification'; // If you have specific types to export
// For now, only the API object is exported as default.
