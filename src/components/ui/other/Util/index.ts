// src/components/ui/other/Util/index.ts

// Export all functions from util.ts
// This makes them available as, e.g., Utils.getScroll or by direct import: import { getScroll } from '...'
export * from './util';

// Alternatively, if you want to group them under a namespace object similar to how
// some libraries do (though less common for utility modules now with ES6 exports):
/*
import * as utils from './util';
export const Util = utils;
*/
