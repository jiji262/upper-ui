// src/components/ui/data-entry/TreeSelect/index.ts
export { default } from './TreeSelect';
export type { TreeSelectOption } from './TreeSelect';
// TreeNode is internal for now, not exporting its props unless needed publicly.
